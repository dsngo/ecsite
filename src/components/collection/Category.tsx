import * as React from "react";
import { connect } from "react-redux";

interface ICategory {}
class Category extends React.Component<ICategory> {
    render() {
        return (
            <div></div>
        )
    }
}

export default Category;